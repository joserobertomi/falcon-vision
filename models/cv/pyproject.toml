[project]
name = "cv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mediapipe",
    "opencv-python",
    "numpy",
    "pillow",
    "ultralytics>=8.3.214",
]
